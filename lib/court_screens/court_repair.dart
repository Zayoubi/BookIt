import 'package:flutter/material.dart';

class CourtRepair extends StatelessWidget {
  const CourtRepair({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
